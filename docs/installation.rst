============
Installation
============

At the command line::

    $ easy_install graphene-jwt-auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv graphene-jwt-auth
    $ pip install graphene-jwt-auth
